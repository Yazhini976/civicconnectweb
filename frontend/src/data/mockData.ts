// Minimal stub — these were used as imports but not actively referenced.
// Kept to satisfy TypeScript imports without holding any real data.

export const CONFIG = {
  wards: [] as string[],
  zones: [] as string[],
  stationTypes: [] as string[],
};

export const generateSTPData = () => [];
